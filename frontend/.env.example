# Umi Max 前端环境变量示例（复制为 .env.local 后本地覆写；.env 已被 gitignore）
# 端口（Umi 读 PORT，避免与后端 8000 冲突）
PORT=5173

# API base url：开发态走相对 /api，由 config.ts 的 proxy 转发到后端 8000
# 生产构建时改为真实后端地址，如 http://<host>:8000/api
UMI_APP_API_BASE_URL=/api

# 开发态身份（后端通过 X-User-Id 识别用户）
UMI_APP_USER_ID=demo-user
